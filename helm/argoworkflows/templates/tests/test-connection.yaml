apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "argoworkflows.fullname" . }}-test-connection"
  labels:
    {{- include "argoworkflows.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "argoworkflows.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
